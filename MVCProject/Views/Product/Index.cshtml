@model IEnumerable<MVCProject.Models.Product>

@{
    ViewBag.Title = "Danh sách";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Sản phẩm</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create") :::: @Html.ActionLink("Thêm giá sản phẩm", "Create", "ProductPrice") | 
    @Html.ActionLink("Cấu hình mã sản phẩm", "Create", "ProductCode")
</p>
<table class="table">
    <tr>
        <th>
            Tên sản phẩm
        </th>
        <th>
            Mã sản phẩm
        </th>
        <th>
            Mã danh mục
        </th>
        <th>
            SKU
        </th>
        <th>
            Nhà cung cấp
        </th>
        <th>
            Hiện/Ẩn
        </th>
        <th>
            Ngày tạo
        </th>
        <th>
            ĐVT
        </th>
        <th>
           BH
        </th>
        <th>
           Đã xóa
        </th>
        
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.ProductName, "Edit", new { id = item.ID })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ItemCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CatID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SKU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SupplierID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Show)
        </td>
        <td>
            @if (item.DateCreate != null)
            {
                DateTime.Parse(item.DateCreate.ToString()).ToString("dd/MM/yyyy");
            }
            &nbsp;
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Unit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Warranty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsDel)
        </td>
        
    </tr>
}

</table>
